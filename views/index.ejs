<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MY APP JRPeppers</title>
    <link rel="stylesheet" href = "/styles.css"/>
</head>
<body>
  <h1 data-position="header">May Node and Express be with you.</h1>

  <section data-position="add-quote">
    <h2>Add quote</h2>


    <form action ="/quotes" method="POST">
        <input type = "text" placeholder ="name" name = "name" >
        <input type = "text" placeholder = "quote" name = "quote">
        <button type="submit">Submit</button>
    </form>
<!-- Form :    The method tells browsers what kind of request to send. In this case, we use POST because we’re sending a POST request.    The action attribute tells the browser where to send the POST request. In this case, we’re send the POST request to /quotes. We can handle this POST request with a post method in server.js. The path path should be the value you placed in the action attribute.-->
  </section>


<!-- the next paragrah replaces<%= quotes %> .  this is embedded javascript loop-->
<section data-position="quotes">
<h2> Quotes </h2>   
<ul class="quotes">
  <!-- Loop through quotes -->
  <% for(var i = 0; i < quotes.length; i++) {%>
    <li class="quote">
      <!-- Output name from the iterated quote object -->
      <span><%= quotes[i].name %></span>:
      <!-- Output quote from the iterated quote object -->
      <span><%= quotes[i].quote %></span>
    </li>
  <% } %>
</ul> 
</section>

<section data-position="update-quote">
<div>
    <h2>Darth Vader invades!</h2>
    <p>
      Replace the first Yoda quote with a quote written by Darth Vader.
    </p>
    <button id="update-button">Replace Yoda's quote</button>
  </div>
</section>

  <section data-position="delete-quote">
  <div>
    <h2>Remove Darth Vader!</h2>
    <p>
      Delete one Darth Vader's quote. Does nothing if there are no more Darth
      Vader's quote
    </p>
    <button id="delete-button">Delete Darth Vader's quote</button>
  </div>
  </section>
  
  <div id="message"></div>


  <!-- next line added during UPDATE section. its okay that public is not in file path for some reason -->
  <script src="/main.js"></script>  

</body>
</html>